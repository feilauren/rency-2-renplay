# Copy this file to .env and fill in your values.
# Only needed if your CrewAI setup is NOT already configured with API keys.
# If CrewAI is pre-configured (e.g. via CREWAI_ANTHROPIC_API_KEY env var), leave blank.

ANTHROPIC_API_KEY=sk-ant-...
